# Claude Code Guidance

`SPEC.md` is the product source of truth.
`AGENTS.md` is the canonical repo guidance for implementation and review.

This file stays intentionally thin so the repo rules do not drift across two
near-duplicate documents.

## Claude-Specific Notes

- Follow `AGENTS.md` for stack, architecture, scoring, ingestion, provider, and
  verification requirements.
- Keep product copy focused on a compact source-grounded quiz tool.
- Prefer the smallest coherent vertical slice and verify it with the smallest
  direct command that fits the current repo state.
